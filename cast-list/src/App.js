import "./App.css";

const App = ({}) => {
	const title = "Generic title";

	return (
		<div className="castSheet">
			<div className="castSheet__title">{title}</div>
		</div>
	);
};

export default App;
